import '';

class ExpenseData {
  // list of all expenses
  ExpenseItem overallExpense = [];

  // get expense list

  // add new expense

  // delete expense

  // get weekday (sun, mon, tue, etc) from DateTime object

  // get the first weekday (mon)
}